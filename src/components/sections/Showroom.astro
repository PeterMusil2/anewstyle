---
import Icon from "../icons/Icon.astro";
import { useTranslations, type Lang } from "../../i18n/utils";
import { getGalleryItems } from "../../lib/cloudinary-gallery";

interface Props {
  id: string;
  lang?: Lang;
}

const { id, lang = "cs" } = Astro.props;
const t = useTranslations(lang);

const youtubeVideos = [
  "PggSmLChjYw",
  "GUiX1ZVmJac",
  "ppa7Y319nE8",
  "XTukH1ix6Fo",
  "LJwBvcEK06M",
];

// Images and videos loaded from the Cloudinary `AnewStyle/Gallery` folder at
// build time. Falls back to an empty list when the JSON hasn't been generated.
const cloudinaryItems = await getGalleryItems();

// 360° panoramas open in a lazy-loaded Photo Sphere Viewer overlay.
const panoLabels = {
  close: lang === "en" ? "Close" : "Zavřít",
  prev: lang === "en" ? "Previous panorama" : "Předchozí panorama",
  next: lang === "en" ? "Next panorama" : "Další panorama",
};
---

<section id={id} class="light">
  <div class="container">
    <h2 class="title text-center title--mg-s">{t("gallery.title")}</h2>
    <p class="desc text-center">
      <span>{t("gallery.descPart1")}</span>
      <strong>{t("gallery.descPart2")}</strong>
      <span>{t("gallery.descPart3")}</span>
      <strong>{t("gallery.descPart4")}</strong>
      <span>{t("gallery.descPart5")}</span>
    </p>
  </div>
  <div class="container segment-filter-wrap">
    <div
      class="segment-filter segment-filter--fit"
      role="group"
      aria-label={t("gallery.filterAriaLabel")}
    >
      <button
        type="button"
        class="segment-filter__btn"
        data-filter=""
        aria-pressed="true"
      >
        <span>{t("gallery.filterAll")}</span>
      </button>
      <button
        type="button"
        class="segment-filter__btn"
        data-filter="video"
        aria-pressed="false"
      >
        <span>{t("gallery.filterVideos")}</span>
      </button>
      <button
        type="button"
        class="segment-filter__btn"
        data-filter="image"
        aria-pressed="false"
      >
        <span>{t("gallery.filterImages")}</span>
      </button>
      <button
        type="button"
        class="segment-filter__btn"
        data-filter="360"
        aria-pressed="false"
      >
        <span>{t("gallery.filter360")}</span>
      </button>
    </div>
  </div>
  <div
    class="container gallery"
    data-pano-close={panoLabels.close}
    data-pano-prev={panoLabels.prev}
    data-pano-next={panoLabels.next}
  >
    <a
      href="/assets/ATAK_bazen_2.webm"
      aria-label={t("products.stoneAsLighting")}
      data-fancybox="gallery"
      data-media="video"
      class="gitem"
      rel="noopener"
    >
      <div class="gitem__photo">
        <img
          src="/assets/ATAK_bazen_2.avif"
          loading="lazy"
          width="768"
          height="446"
          alt={t("gallery.demoVideo")}
        />
      </div>
      <div class="gitem__content">
        <Icon name="play" />
      </div>
    </a>

    <a
      href="/videos/Rock_sheet_animation_background.mp4"
      aria-label={t("products.stoneAsLighting")}
      data-fancybox="gallery"
      data-media="video"
      class="gitem"
      rel="noopener"
    >
      <div class="gitem__photo">
        <picture>
          <source
            type="image/webp"
            sizes="(min-width: 426px)"
            srcset="/images/gallery/Rockfoil_viz1_video_768.webp"
          />
          <source
            type="image/webp"
            sizes="(max-width: 425px)"
            srcset="/images/gallery/Rockfoil_viz1_video_425.webp"
          />
          <img
            src="/images/gallery/Rockfoil_viz1_video_425.webp"
            loading="lazy"
            width="768"
            height="446"
            alt={t("gallery.demoVideo")}
          />
        </picture>
      </div>
      <div class="gitem__content">
        <Icon name="play" />
      </div>
    </a>

    {
      youtubeVideos.map((videoId) => (
        <a
          href={`https://youtu.be/${videoId}`}
          aria-label={t("gallery.youtubeVideo")}
          data-fancybox="gallery"
          data-media="video"
          class="gitem"
          rel="noopener"
        >
          <div class="gitem__photo">
            <img
              src={`https://i.ytimg.com/vi/${videoId}/hqdefault.jpg`}
              loading="lazy"
              width="480"
              height="360"
              alt={t("gallery.youtubeVideo")}
            />
          </div>
          <div class="gitem__content">
            <Icon name="play" />
          </div>
        </a>
      ))
    }

    {
      cloudinaryItems.map((item) =>
        item.type === "360" ? (
          <a
            href={item.full}
            aria-label={item.alt ?? t("gallery.preview360Day")}
            data-media="360"
            data-pano={item.full}
            class="gitem"
            rel="noopener"
          >
            <div class="gitem__photo">
              <img
                src={item.thumb}
                loading="lazy"
                width={item.width}
                height={item.height}
                alt={item.alt ?? t("gallery.preview360Day")}
              />
            </div>
            <div class="gitem__content">
              <Icon name="360" />
            </div>
          </a>
        ) : (
          <a
            href={item.full}
            aria-label={item.alt ?? t("products.stoneAsLighting")}
            data-fancybox="gallery"
            data-media={item.type}
            class="gitem"
            rel="noopener"
          >
            <div class="gitem__photo">
              <img
                src={item.thumb}
                loading="lazy"
                width={item.width}
                height={item.height}
                alt={item.alt ?? t("products.stoneAsLighting")}
              />
            </div>
            {item.type === "video" && (
              <div class="gitem__content">
                <Icon name="play" />
              </div>
            )}
          </a>
        )
      )
    }

    <a
      href="https://www.theasys.io/viewer/52yTAK9fAdceRkI8KoRZLYYkLgPkaq"
      aria-label={t("products.stoneAsLighting")}
      data-fancybox="gallery"
      data-media="360"
      class="gitem"
      rel="noopener"
      data-type="iframe"
    >
      <div class="gitem__photo">
        <img
          src="/images/gallery/360-day.webp"
          loading="lazy"
          width="425"
          height="247"
          alt={t("gallery.preview360Day")}
        />
      </div>
      <div class="gitem__content">
        <Icon name="360" />
      </div>
    </a>
    <a
      href="https://www.theasys.io/viewer/BRgTqQK9OimeTDIPcLmzi4WLP3OIBU"
      aria-label={t("products.stoneAsLighting")}
      data-fancybox="gallery"
      data-media="360"
      class="gitem"
      rel="noopener"
      data-type="iframe"
    >
      <div class="gitem__photo">
        <img
          src="/images/gallery/360-night.webp"
          loading="lazy"
          width="425"
          height="247"
          alt={t("gallery.preview360Night")}
        />
      </div>
      <div class="gitem__content">
        <Icon name="360" />
      </div>
    </a>

    <a
      href="/images/gallery/Rockfoil_viz2_1440.webp"
      aria-label={t("products.stoneAsLighting")}
      data-fancybox="gallery"
      data-media="image"
      class="gitem"
      rel="noopener"
    >
      <div class="gitem__photo">
        <img
          src="/images/gallery/Rockfoil_viz2_425.webp"
          loading="lazy"
          width="425"
          height="247"
          alt={t("products.stoneAsLighting")}
        />
      </div>
      <div class="gitem__content">
        <strong>PINK</strong>
      </div>
    </a>
    <a
      href="/images/gallery/Rockfoil_viz3_1440.webp"
      aria-label={t("products.stoneAsLighting")}
      data-fancybox="gallery"
      data-media="image"
      class="gitem"
      rel="noopener"
    >
      <div class="gitem__photo">
        <img
          src="/images/gallery/Rockfoil_viz3_425.webp"
          loading="lazy"
          width="425"
          height="247"
          alt={t("products.stoneAsLighting")}
        />
      </div>
      <div class="gitem__content">
        <strong>DARKGOLD</strong>
      </div>
    </a>
    <a
      href="/images/gallery/Rockfoil_viz4_1440.webp"
      aria-label={t("products.stoneAsLighting")}
      data-fancybox="gallery"
      data-media="image"
      class="gitem"
      rel="noopener"
    >
      <div class="gitem__photo">
        <img
          src="/images/gallery/Rockfoil_viz4_425.webp"
          loading="lazy"
          width="425"
          height="247"
          alt={t("products.stoneAsLighting")}
        />
      </div>
      <div class="gitem__content">
        <strong>SUNLIGHT</strong>
      </div>
    </a>
    <a
      href="/images/gallery/Rockfoil_viz8_1440.webp"
      aria-label={t("products.stoneAsLighting")}
      data-fancybox="gallery"
      data-media="image"
      class="gitem"
      rel="noopener"
    >
      <div class="gitem__photo">
        <img
          src="/images/gallery/Rockfoil_viz8_425.webp"
          loading="lazy"
          width="425"
          height="247"
          alt={t("products.stoneAsLighting")}
        />
      </div>
      <div class="gitem__content">
        <strong>SUNLIGHT</strong>
      </div>
    </a>
    <a
      href="/images/gallery/Rockfoil_viz9_1440.webp"
      aria-label={t("products.stoneAsLighting")}
      data-fancybox="gallery"
      data-media="image"
      class="gitem"
      rel="noopener"
    >
      <div class="gitem__photo">
        <img
          src="/images/gallery/Rockfoil_viz9_425.webp"
          loading="lazy"
          width="425"
          height="247"
          alt={t("products.stoneAsLighting")}
        />
      </div>
      <div class="gitem__content">
        <strong>GOLDENBARK</strong>
      </div>
    </a>
    <a
      href="/images/gallery/Rockfoil_viz7_1440.webp"
      aria-label={t("products.stoneAsLighting")}
      data-fancybox="gallery"
      data-media="image"
      class="gitem"
      rel="noopener"
    >
      <div class="gitem__photo">
        <img
          src="/images/gallery/Rockfoil_viz7_425.webp"
          loading="lazy"
          width="425"
          height="247"
          alt={t("products.stoneAsLighting")}
        />
      </div>
      <div class="gitem__content">
        <strong>SUNLIGHT</strong>
      </div>
    </a>
    <a
      href="/images/gallery/Rockfoil_viz5_1440.webp"
      aria-label={t("products.stoneAsLighting")}
      data-fancybox="gallery"
      data-media="image"
      class="gitem hidden"
      rel="noopener"
    >
    </a>
    <a
      href="/images/gallery/Rockfoil_viz6_1440.webp"
      aria-label={t("products.stoneAsLighting")}
      data-fancybox="gallery"
      data-media="image"
      class="gitem hidden"
      rel="noopener"
    >
    </a>
  </div>
  <div class="container">
    <p class="desc desc--mg-s desc--center">
      <span>{t("gallery.underText")}</span>
      <a
        href="https://www.designatak.cz/"
        aria-label={t("gallery.gotoAtakDesign")}
        class="link"
        style="text-decoration: underline;"
        rel="external"
      >
        ATAK Design
      </a>.
      <br /><br />
      <picture class="atak-logo">
        <source
          type="image/avif"
          sizes="(min-width: 100px)"
          srcset="/images/logos/atak-logo.avif"
        />
        <source
          type="image/webp"
          sizes="(min-width: 100px)"
          srcset="/images/logos/atak-logo.webp"
        />
        <img
          src="/images/logos/atak-logo.webp"
          loading="lazy"
          width="250"
          alt={t("common.atakLogoAlt")}
        />
      </picture>
    </p>
  </div>
</section>

<script>
  const initGalleryFilter = () => {
    const gallery = document.querySelector<HTMLElement>(".gallery");
    const filter = gallery
      ?.closest("section")
      ?.querySelector<HTMLElement>(".segment-filter");
    if (!filter || !gallery) return;

    const buttons = filter.querySelectorAll<HTMLButtonElement>(
      ".segment-filter__btn"
    );

    buttons.forEach((btn) => {
      btn.addEventListener("click", () => {
        if (btn.getAttribute("aria-pressed") === "true") return;
        buttons.forEach((b) => b.setAttribute("aria-pressed", "false"));
        btn.setAttribute("aria-pressed", "true");

        const filter = btn.dataset.filter ?? "";
        if (filter) {
          gallery.setAttribute("data-filter", filter);
        } else {
          gallery.removeAttribute("data-filter");
        }
      });
    });
  };

  initGalleryFilter();
  document.addEventListener("astro:page-load", initGalleryFilter);
</script>

<script>
  // Lazy 360° panorama viewer. Photo Sphere Viewer (WebGL/three.js) and its CSS
  // are only fetched the first time a visitor opens a panorama. Prev/next cycle
  // through every panorama in the gallery by swapping the source on one viewer.
  const initPanoViewer = () => {
    const triggers = Array.from(
      document.querySelectorAll<HTMLAnchorElement>("a[data-pano]")
    );
    if (triggers.length === 0) return;

    const urls = triggers
      .map((trigger) => trigger.dataset.pano ?? "")
      .filter(Boolean);
    const gallery = document.querySelector<HTMLElement>(".gallery");
    const labels = {
      close: gallery?.dataset.panoClose ?? "Close",
      prev: gallery?.dataset.panoPrev ?? "Previous",
      next: gallery?.dataset.panoNext ?? "Next",
    };

    let closeActive: (() => void) | null = null;

    const button = (
      className: string,
      label: string,
      glyph: string
    ): HTMLButtonElement => {
      const btn = document.createElement("button");
      btn.type = "button";
      btn.className = className;
      btn.setAttribute("aria-label", label);
      btn.innerHTML = glyph;
      return btn;
    };

    const open = async (startIndex: number): Promise<void> => {
      let index = startIndex;

      const modal = document.createElement("div");
      modal.className = "pano-modal";
      modal.setAttribute("role", "dialog");
      modal.setAttribute("aria-modal", "true");

      const stage = document.createElement("div");
      stage.className = "pano-modal__stage";

      const closeBtn = button("pano-modal__close", labels.close, "&times;");
      modal.append(stage, closeBtn);

      const multiple = urls.length > 1;
      const prevBtn = multiple
        ? button("pano-modal__nav pano-modal__nav--prev", labels.prev, "&lsaquo;")
        : null;
      const nextBtn = multiple
        ? button("pano-modal__nav pano-modal__nav--next", labels.next, "&rsaquo;")
        : null;
      if (prevBtn && nextBtn) modal.append(prevBtn, nextBtn);

      document.body.append(modal);
      document.documentElement.style.overflow = "hidden";

      const [{ Viewer }] = await Promise.all([
        import("@photo-sphere-viewer/core"),
        import("@photo-sphere-viewer/core/index.css"),
      ]);

      const viewer = new Viewer({
        container: stage,
        panorama: urls[index],
        navbar: ["zoom", "move", "fullscreen"],
      });

      const go = (delta: number): void => {
        index = (index + delta + urls.length) % urls.length;
        void viewer.setPanorama(urls[index], { showLoader: true });
      };

      const onKey = (event: KeyboardEvent): void => {
        if (event.key === "Escape") close();
        else if (event.key === "ArrowLeft" && multiple) go(-1);
        else if (event.key === "ArrowRight" && multiple) go(1);
      };

      const close = (): void => {
        document.removeEventListener("keydown", onKey);
        viewer.destroy();
        modal.remove();
        document.documentElement.style.overflow = "";
        closeActive = null;
      };

      closeBtn.addEventListener("click", close);
      prevBtn?.addEventListener("click", () => go(-1));
      nextBtn?.addEventListener("click", () => go(1));
      modal.addEventListener("click", (event) => {
        if (event.target === modal) close();
      });
      document.addEventListener("keydown", onKey);
      closeActive = close;
    };

    triggers.forEach((trigger, triggerIndex) => {
      if (trigger.dataset.panoBound) return;
      trigger.dataset.panoBound = "1";
      trigger.addEventListener("click", (event) => {
        if (!trigger.dataset.pano) return;
        event.preventDefault();
        closeActive?.();
        void open(triggerIndex);
      });
    });
  };

  initPanoViewer();
  document.addEventListener("astro:page-load", initPanoViewer);
</script>
