# Cloudinary credentials for the build-time gallery fetch.
# Copy to `.env` (git-ignored) or set in your CI / host environment.
# Format: cloudinary://API_KEY:API_SECRET@CLOUD_NAME
# The gallery reads images and videos from the `AnewStyle/Gallery` folder.
CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME
